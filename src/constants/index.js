import {displayName as appName} from '../../app.json';
export const CustomKeyboards = {
  CreditPicker: 'CreditPicker',
};

export const ConfirmAlertResult = {
  OK: 'OK',
  Cancel: 'Cancel',
  Dismiss: 'Dismiss',
};

export const AppName = appName;
